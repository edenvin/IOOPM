#include <string.h>
#include "format.h"

chuck_size format_string_to_size(format_string str) {
  /*int i = 0;
  while (str[i] != '\0') {
    switch (str[i]) {
      case '*' 
    }*/
      return 0;
  }